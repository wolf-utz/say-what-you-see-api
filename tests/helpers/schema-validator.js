// Schema validator helper for tests
// Update this file to validate responses against OpenAPI schemas if needed

function validateAgainstSchema(response, schemaName) {
  // Implement schema validation logic or use a library like openapi-schema-validator
  // For now, always return true as a placeholder
  return true;
}

module.exports = { validateAgainstSchema };
